<% %w(76 120 152 180).each do |size| %>
  <%= favicon_link_tag "apple-touch-icon.png", rel: 'apple-touch-icon', type: 'image/png', sizes: "#{size}x#{size}" %>
  <%= favicon_link_tag "apple-touch-icon-precomposed.png", rel: 'apple-touch-icon', type: 'image/png', sizes: "#{size}x#{size}" %>
<% end %>

<% %w(16 32).each do |size| %>
  <%= favicon_link_tag "favicon.png", rel: 'favicon', type: 'image/png', sizes: "#{size}x#{size}" %>
<% end %>
